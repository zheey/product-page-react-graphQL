import React from "react";
import Overlay from "../common/Overlay";
import CartForm from "../common/CartForm";

const Cart = () => {
    return(
        <Overlay>
            <CartForm/>
        </Overlay>
    )
};

export default Cart;